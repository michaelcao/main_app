<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= image_tag(@user.avatar.url(:thumb), alt: @user.name, class: "avatar", size: "150x150") %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>